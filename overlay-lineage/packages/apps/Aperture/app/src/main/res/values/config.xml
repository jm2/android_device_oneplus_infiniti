<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: 2022 The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <!-- Use the logical multi-camera API (single back device, lenses chosen by zoom ratio) rather
         than exposing the physical back lenses as separate aux cameras. The back camera (ID 0) is a
         LOGICAL_MULTI_CAMERA spanning ultrawide/main/tele; with logicalZoomRatios referencing it all
         physical back aux devices are ignored automatically (see config_logicalZoomRatios below). -->
    <bool name="config_enableAuxCameras">false</bool>

    <!-- List of ID of auxiliary cameras that must be ignored by the app. -->
     <string-array name="config_ignoredAuxCameraIds">
     </string-array>

    <!-- An array of triplets made of (camera ID, qualities, framerates).
         These video modes will be added to the available
         quality/framerate combinations of a camera device.
         Make sure the device is able to handle those configurations
         and maintain a stable framerate at any condition.
         Note that you can't add video qualities that aren't
         exposed by the camera, only new framerates.
         Valid values of resolution are:
          - "sd" (480p)
          - "hd" (720p)
          - "fhd" (1080p)
          - "uhd" (2160p)
         Valid values of framerate are:
          - "24"
          - "30"
          - "60"
          - "120"
         Example:
             <string-array name="config_additionalVideoConfigurations">
                 <item>0</item> <item>sd|hd|fhd</item> <item>60|120</item>
                 <item>1</item> <item>sd|hd|fhd</item> <item>60</item>
             </string-array>
    -->
    <string-array name="config_additionalVideoConfigurations">
        <item>0</item> <item>sd|hd|fhd|uhd</item> <item>60|120</item>
        <item>1</item> <item>sd|hd|fhd</item>     <item>60</item>
    </string-array>

    <!-- Lens selector for the logical back camera (ID 0). Topology from dumpsys media.camera;
         zoom ratios are 35mm-equivalents (raw focal-length ratios are wrong here because the three
         back sensors are different sizes). Per-sensor physicalSize -> crop factor (43.27mm FF diag):
           main      (cam 2): 8.192x6.144mm diag 10.24, f=5.59  -> crop 4.23 -> 23.6mm  -> 1.00x
           ultrawide (cam 3): 5.014x3.760mm diag  6.27, f=2.31  -> crop 6.90 -> 16.0mm  -> 0.67x
           tele      (cam 4): 5.243x3.932mm diag  6.55, f=12.19 -> crop 6.60 -> 80.5mm  -> 3.41x
         These match the OP15 spec sheet (24 / 16 / 80mm; tele 3.5x) and ultrawide's 0.67x is exactly
         the logical zoomRatioRange minimum. Triplets: cameraId / approximate ratio (button label) /
         exact zoom ratio that engages the lens. 1.0x (main) is always added implicitly by Aperture.
         Tele exact set to 3.5 (just past the 3.41x optical crossover) to guarantee the tele lens. -->
    <string-array name="config_logicalZoomRatios">
        <item>0</item> <item>0.6</item> <item>0.67</item>
        <item>0</item> <item>3.5</item> <item>3.5</item>
    </string-array>
</resources>
